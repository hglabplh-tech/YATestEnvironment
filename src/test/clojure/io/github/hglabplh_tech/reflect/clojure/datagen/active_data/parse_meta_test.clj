(ns io.github.hglabplh_tech.reflect.clojure.datagen.active-data.parse-meta-test
  (:require [clojure.test :refer :all]

            [io.github.hglabplh_tech.reflect.clojure.datagen.active-data.parse-meta :refer :all]
            )
  )

()

(deftest structured-def-rec-schema.test (testing "The conversion of the active data - meta-schema records to structured output")
                                          (get-data-active-meta val)

                                          )
